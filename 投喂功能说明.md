# 三个投喂功能的区别分析

## 数据库现状
项目数据库里有两张投喂相关表，但后端只实现了 feeding_points 的 API：

### feeding_points 表（固定投喂点）
| 字段 | 说明 |
|------|------|
| name | 投喂点名称（如"图书馆后面"） |
| latitude/longitude | 位置坐标 |
| description | 描述 |
| created_by | 创建人 |

### feeding_events 表（投喂事件）
| 字段 | 说明 |
|------|------|
| feeding_point_id | 关联到哪个投喂点 |
| feeder_id | 谁投喂的 |
| food_type | 投喂了啥（猫粮/罐头等） |
| amount | 投喂量 |
| note | 备注 |

---

## 功能 4：投喂点地图展示
**在地图上显示所有固定投喂点的标记**

已有：GET /api/feeding-points（后端已有）
缺的：前端地图上显示投喂点标记

用户打开首页地图，能看到：
- 猫咪位置（猫爪图标）
- 投喂点位置（食盆图标）
点击投喂点能看到名称和描述

## 功能 5：投喂点管理页面
**管理投喂点（增删改查）**

已有：GET 和 POST（后端已有）
缺的：PUT（修改）、DELETE（删除）、前端管理页面

需要一个单独的管理页面：
- 列出所有投喂点
- 新增投喂点（名称、位置、描述）
- 编辑投喂点
- 删除投喂点

## 功能 6：投喂事件记录
**记录"谁在哪个投喂点投喂了什么"**

后端完全没实现！需要新建路由：
- POST /api/feeding-events（记录投喂）
- GET /api/feeding-events（查询记录）

前端需要一个记录页面：
- 选择一个投喂点
- 填写食物类型、数量
- 提交记录

---

## 我的建议

这三个功能是递进关系，但功能 6（事件记录）和现有的"投喂打卡"有重复。

**现状问题：** 现在的「投喂打卡」是往 sightings 表写 [feeding] 标记，
跟 feeding_points / feeding_events 两张表完全无关。
所以如果要实现这三个功能，相当于新建一套投喂体系。

**推荐优先实现：**
1. ④ 地图展示（最直观，改动最小）
2. ⑥ 事件记录（真正记录投喂数据）
3. ⑤ 管理页面（可以通过数据库直接管理，不急）
